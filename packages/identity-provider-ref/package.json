{
	"name": "@refarm.dev/identity-provider-ref",
	"version": "0.1.0",
	"description": "Sovereign identity provider (WASM): the Ed25519 private key is generated and held inside the sandbox and never crosses the boundary.",
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"exports": {
		".": {
			"import": "./dist/index.js",
			"types": "./dist/index.d.ts"
		}
	},
	"scripts": {
		"build:wasm": "node ../../scripts/ci/cargo-run.mjs --copy wasm32-wasip1/release/refarm_identity_provider_ref.wasm dist/identity_provider.wasm component build --target wasm32-wasip1 --release",
		"build:transpile": "jco transpile dist/identity_provider.wasm -o pkg --name identity_provider --no-wasi-shim --instantiation async",
		"build:component": "node ../../scripts/ci/cargo-run.mjs --copy wasm32-wasip1/release/refarm_identity_provider_ref.wasm dist/identity_provider.wasm component build --target wasm32-wasip1 --release && jco transpile dist/identity_provider.wasm -o pkg --name identity_provider --no-wasi-shim --instantiation async",
		"build": "tsc --project tsconfig.build.json",
		"type-check": "tsc --noEmit",
		"test": "vitest run",
		"test:rust": "node ../../scripts/ci/cargo-run.mjs test -- --test-threads=1"
	},
	"files": [
		"pkg",
		"dist",
		"!dist/**/*.tsbuildinfo",
		"README.md"
	],
	"publishConfig": {
		"access": "public"
	},
	"dependencies": {
		"@refarm.dev/identity-contract-v1": "workspace:*"
	},
	"devDependencies": {
		"@bytecodealliance/jco": "catalog:",
		"@refarm.dev/deps": "workspace:*",
		"@refarm.dev/tsconfig": "workspace:*",
		"typescript": "catalog:"
	}
}
