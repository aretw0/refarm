{
	"name": "@refarm.dev/delegate",
	"version": "0.1.0",
	"type": "module",
	"publishConfig": {
		"access": "public"
	},
	"description": "delegate — the agent-delegation ergonomics plugin: single + chain persona pipelines over the agent's respond, via call_plugin. Personas are sovereign AgentPersona graph nodes.",
	"scripts": {
		"build": "node ../../scripts/ci/cargo-run.mjs --copy wasm32-wasip1/release/delegate.wasm dist/plugin.wasm component build --target wasm32-wasip1 --release && node scripts/write-plugin-manifest.mjs",
		"build:wasm": "node ../../scripts/ci/cargo-run.mjs --copy wasm32-wasip1/release/delegate.wasm dist/plugin.wasm component build --target wasm32-wasip1 --release && node scripts/write-plugin-manifest.mjs",
		"test": "node ../../scripts/ci/cargo-run.mjs test -- --test-threads=1",
		"test:unit": "node ../../scripts/ci/cargo-run.mjs test -- --test-threads=1",
		"test:integration": "echo 'No integration tests for this crate'"
	},
	"files": [
		"dist/plugin.wasm",
		"dist/plugin.json"
	],
	"devDependencies": {
		"@refarm.dev/deps": "workspace:*",
		"@refarm.dev/plugin-manifest": "workspace:*"
	},
	"license": "AGPL-3.0-only"
}
