# @refarm.dev/storage-rest

A REST-based remote storage adapter for Refarm. Enables the microkernel to synchronize with and store data on remote HTTP servers.

## Role

It implements the `@refarm.dev/storage-contract-v1` and translates graph operations into RESTful API calls.
