{
  "name": "@refarm.dev/model-catalog-plugin-stack",
  "version": "0.1.0",
  "private": true,
  "description": "Plugin-of-plugins composer for model rate catalogs",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "dev": "tsc --project tsconfig.build.json --watch",
    "lint": "eslint src",
    "type-check": "tsc --noEmit",
    "type-check:dist": "tsc --project tsconfig.build.json --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run src/index.test.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@refarm.dev/model-catalog-v1": "workspace:*",
    "@refarm.dev/model-catalog-plugin-anthropic": "workspace:*",
    "@refarm.dev/model-catalog-plugin-openai": "workspace:*"
  },
  "devDependencies": {
    "@refarm.dev/eslint-config": "workspace:*",
    "@refarm.dev/tsconfig": "workspace:*",
    "@refarm.dev/vtconfig": "workspace:*"
  }
}
