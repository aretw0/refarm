{
	"name": "@refarm.dev/health",
	"version": "0.1.0",
	"description": "The Health — Deterministic project diagnostics for Refarm.",
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"scripts": {
		"build": "tsc --project tsconfig.build.json",
		"dev": "tsc --project tsconfig.build.json --watch",
		"test": "vitest run --passWithNoTests",
		"lint": "eslint src"
	},
	"dependencies": {
		"isomorphic-git": "catalog:"
	},
	"license": "AGPL-3.0-only",
	"publishConfig": {
		"access": "public"
	},
	"exports": {
		".": {
			"import": "./dist/index.js",
			"types": "./dist/index.d.ts"
		}
	},
	"devDependencies": {
		"@refarm.dev/eslint-config": "workspace:*",
		"@refarm.dev/deps": "workspace:*"
	}
}