// Scaffold placeholder. The kernel modules (registry, types, surface-model, the
// projectors, envelope) are moved here from @refarm.dev/cli in the next commit
// via `git mv` so history is preserved. This file is replaced by that move.
export {};
