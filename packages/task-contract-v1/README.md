# @refarm.dev/task-contract-v1

Versioned task capability contract (`task:v1`) and conformance helpers.

## Exports

- `TASK_CAPABILITY`
- `runTaskV1Conformance(adapter)`
- `createInMemoryTaskAdapter()`
- Task contract types (`Task`, `TaskEvent`, `TaskContractAdapter`, ...)
