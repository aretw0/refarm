/**
 * Public package root: contracts that load in any Node or browser host without bundler aliases.
 * The web-shell aggregate remains on `@refarm.dev/homestead/sdk`; Astro UI remains on `./ui`.
 */
export * from "./sdk/host-renderer.js";
