package refarm:plugin@0.1.0;

// agent world — imports integration/host interfaces from refarm-plugin-wit
// so the exported interface remains `refarm:plugin/integration@0.1.0`.
world agent {
    import tractor-bridge;
    import model-bridge;
    import agent-fs;
    import agent-shell;
    import structured-io;
    import code-ops;
    export integration;
}
