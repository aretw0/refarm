{
	"name": "@refarm.dev/operation-consent-v1",
	"version": "0.1.0",
	"private": true,
	"description": "An operation is proposed as an exact diff, authorised by the operator, applied, and remembered with its undo",
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"exports": {
		".": {
			"import": "./dist/index.js",
			"types": "./dist/index.d.ts"
		}
	},
	"scripts": {
		"build": "tsc --project tsconfig.build.json",
		"dev": "tsc --project tsconfig.build.json --watch",
		"type-check": "tsc --noEmit",
		"test": "vitest run",
		"clean": "rm -rf dist",
		"test:unit": "vitest run",
		"lint": "eslint src"
	},
	"devDependencies": {
		"@refarm.dev/tsconfig": "workspace:*",
		"typescript": "catalog:",
		"@refarm.dev/vtconfig": "workspace:*",
		"@refarm.dev/eslint-config": "workspace:*",
		"@refarm.dev/prompt-contract-v1": "workspace:*"
	}
}
