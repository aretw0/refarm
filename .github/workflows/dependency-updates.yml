name: Dependency Updates

on:
  schedule:
    - cron: "0 9 * * 1" # Todas as segundas às 9:00 UTC
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  check-and-update:
    uses: ./.github/workflows/reusable-dependency-update.yml
