# v0.1.0 Release Gates — Daily Driver First

> This document used to split `v0.1.0` into contract publication and private daily-driver stabilization. Recalibration: contract readiness is not enough. Refarm should not mint or publish `v0.1.0` until it is the creator's daily driver and can credibly replace the current external pi workflow.

**Current version**: `v0.0.1-dev`  
**Target**: stabilize Refarm as a daily driver before any `v0.1.0` publication
**Tracking**: [`roadmaps/MAIN.md`](../roadmaps/MAIN.md)

---

## Track A: Contract Publication Inventory (hold)

The 3 foundational contracts may remain useful as release candidates, but publication is on hold until the daily-driver gate passes or there is an explicit human override. Treat the checklist below as inventory, not a launch plan.

### Gate 1: Release Infrastructure Ready ✅ MOSTLY DONE

> See: [`docs/REPOSITORY_MIGRATION_GUIDE.md`](REPOSITORY_MIGRATION_GUIDE.md) and [`docs/POST_TRANSFER_CHECKLIST.md`](POST_TRANSFER_CHECKLIST.md)

- [ ] `RELEASE_AUTOMATION=true` configured in repository variables (GitHub Settings → Variables)
- [ ] `RELEASE_OWNER=aretw0` configured (optional but recommended)
- [ ] npm scope `@aretw0` configured with publish access in package.json files
- [ ] Branch protection rules applied (see [`docs/BRANCH_PROTECTION_SETUP.md`](BRANCH_PROTECTION_SETUP.md))
- [ ] CI/CD secrets updated (`NPM_TOKEN` with publish rights to `@aretw0`)

**Owner**: You  
**Status**: frozen until daily-driver gate

### Gate 2: Contracts Validated ✅ DONE

- [x] `storage-contract-v1` → 6 conformance tests passing
- [x] `sync-contract-v1` → 4 conformance tests passing
- [x] `identity-contract-v1` → 4 conformance tests passing
- [x] All 3 contracts have READMEs with examples
- [x] TypeScript declarations exported
- [x] ESM-only (no dual-bundle complexity)
- [x] Zero runtime dependencies
- [x] `npm publish --dry-run` succeeds for each

> `plugin-manifest` is intentionally deferred to v0.2.0 until Pi + multi-layer plugin architecture is finalized.

**Owner**: Already complete  
**Status**: ✅ Ready

### Gate 3: Documentation ✅ DONE

- [x] Each contract package has a clear README
- [x] Schema examples provided (`identity-contract-v1`, `plugin-manifest`)
- [x] Integration example in `examples/third-party-plugin/`
- [x] [`packages/DISTRIBUTION_STATUS.md`](../packages/DISTRIBUTION_STATUS.md) updated
- [x] [`docs/REFARM_PERSONAL_DAILY_DRIVER.md`](REFARM_PERSONAL_DAILY_DRIVER.md) explains publishing strategy

**Owner**: Just completed  
**Status**: ✅ Ready

### Gate 4: Publish Contracts (deferred)

Do not run this while the daily-driver gate is still open. Historical command sketch retained for reference only:

```bash
# Create annotated tags and push
git tag -a @aretw0/storage-contract-v1@0.1.0 -m "v0.1.0: Storage contract"
git tag -a @aretw0/sync-contract-v1@0.1.0 -m "v0.1.0: Sync contract"
git tag -a @aretw0/identity-contract-v1@0.1.0 -m "v0.1.0: Identity contract"

git push origin @aretw0/storage-contract-v1@0.1.0
git push origin @aretw0/sync-contract-v1@0.1.0
git push origin @aretw0/identity-contract-v1@0.1.0

# CI/CD automatically publishes via publish-packages.yml workflow
```

**Owner**: You  
**Status**: deferred until daily-driver gate or explicit human override

### Gate 5: Verify Publication

- [ ] `npm info @aretw0/storage-contract-v1` shows v0.1.0
- [ ] `npm info @aretw0/sync-contract-v1` shows v0.1.0
- [ ] `npm info @aretw0/identity-contract-v1` shows v0.1.0
- [ ] GitHub Releases created for `v0.1.0-contracts`
- [ ] [`packages/DISTRIBUTION_STATUS.md`](../packages/DISTRIBUTION_STATUS.md) updated with timestamps

**Owner**: You  
**Timeline**: April 16–17, 2026

---

## Track B: Daily Driver Stabilization (You as User)

These gates represent your personal use of Refarm. They don't block contract publication, but they determine when you can rely on Refarm for real work.

### Gate 1: Tractor ↔ Homestead Integration ✅ IN PROGRESS (Gate 3 in original)

> Spec: [docs/gate3-homestead-tractor-spec.md](gate3-homestead-tractor-spec.md)

**Gate 1a: Technical Primitives**

- [ ] `BrowserSyncClient` connects to tractor (`ws://localhost:42000`)
- [ ] Sync roundtrip: write via browser → Loro delta → tractor → OPFS update
- [ ] `installPlugin()` validates SHA-256, writes to OPFS, rejects tampered bundles

**Gate 1b: Reference Distro (`apps/me`)**

- [ ] `StudioShell` boots with Herald + Firefly active in real DOM slots
- [ ] At least 1 sovereign content plugin loaded from OPFS with SHA-256 validated
- [ ] Offline-first confirmed: edit in `apps/me` while tractor is offline, sync on reconnect

**Owner**: You  
**Timeline**: End of April 2026 (Gate 3 from original release plan)

### Gate 2: Plugin Ecosystem (Barn)

- [ ] `barn` package completes SDD/BDD (API contracts finalized)
- [ ] Hot-swap 3+ plugins in `apps/me` without restarting
- [ ] `installPlugin()` robustly handles tampered/expired plugins (reject before execute)
- [ ] Plugin state persists in SQLite even after browser refresh

**Owner**: You + Barn development  
**Timeline**: Late April / Early May 2026

### Gate 3: Test Coverage

- [x] `cargo test -p tractor -- --test-threads=1` → 52/52 ✅
- [ ] `packages/storage-sqlite` → 100% passing
- [ ] `packages/sync-loro` → 100% passing
- [ ] `packages/tractor-ts` → 100% passing

**Owner**: You / CI validation  
**Timeline**: Continuous

### Gate 4: 30-Day Daily Use

**Acceptance Criteria**:

- [ ] You've used `apps/me` + Tractor for 30 consecutive days
- [ ] All GitHub issues for Refarm moved to `/work/refarm` project tree
- [ ] No data loss events or unrecoverable sync corruption
- [ ] At least 1 personal plugin loaded and working
- [ ] Offline → reconnect workflow tested and works
- [ ] Can recover from `.db` backup if needed

**Owner**: You  
**Timeline**: May 15, 2026 (arbitrary milestone)

### Gate 5: Decision — Publish Tractor as Reference Implementation?

**After Gate 4**, decide:

- **Option A**: Publish `@aretw0/tractor` v0.1.0 to npm (reference Rust implementation)
- **Option B**: Keep Tractor private; publish only contracts (lean, low-risk)
- **Option C**: Publish Tractor + Homestead together as "personal distro reference"

**Recommendation**: Option A (publish Tractor mid-2026) after you've used it daily and can speak to stability.

---

## Execution Order

### Week 1 (Apr 14–20): Contracts to npm

1. Complete Gate A-1 (GitHub variables)
2. Verify Gate A-2 + A-3 (already done)
3. Execute Gate A-4 (tag and push)
4. Verify Gate A-5 (npm published)
5. **Result**: `v0.1.0-contracts` live on npm

### Weeks 2–8: Daily Driver

1. Continue Gate B-1 (Tractor ↔ Homestead integration)
2. Complete Gate B-2 (Barn plugin lifecycle)
3. Run Gate B-3 (test coverage)
4. Live Gate B-4 (daily use + issue migration)
5. Decide Gate B-5 (publish Tractor or not)

### Promotion Matrix: dev → me → social

Use this matrix as the operational rule for all new capabilities.

| Domain                           | Build First  | Promote to `me` when                                                  | Promote to `social` when                            |
| -------------------------------- | ------------ | --------------------------------------------------------------------- | --------------------------------------------------- |
| Storage / Sync / Identity        | `refarm.dev` | Tests green + offline/reconnect validated + no data loss in daily use | Interoperability proven across multiple user setups |
| Tractor runtime                  | `refarm.dev` | 30-day stable use + consumer map validated                            | External operators can run it with docs only        |
| Plugin architecture (Pi/WASM/UI) | `refarm.dev` | 3 plugin types working + inter-layer composition validated            | Third-party plugin authors can publish safely       |
| Automation workflows             | `refarm.dev` | Weekly reliability in your real routine                               | Templates are reusable by others                    |
| UI/UX shell (apps/me)            | `refarm.dev` | Zero-friction daily operation for your workload                       | Community-facing defaults and onboarding are stable |

Decision policy:

1. `dev` is the only place for first implementation.
2. `me` is a stability channel, not a prototype channel.
3. `social` only ships patterns that survived real usage in `me`.

---

## What "v0.1.0" Means Now

- **For the Ecosystem**: v0.1.0-contracts are published. "Refarm is a capability contract standard."
- **For You**: You're living in v0.0.1-dev implementations, gradually stabilizing for daily work.
- **Future**: v0.2.0 publishes reference implementations (Tractor, Homestead, plugins) once you've proven them stable.

---

## See Also

- [REFARM_PERSONAL_DAILY_DRIVER.md](REFARM_PERSONAL_DAILY_DRIVER.md) — The "why" behind this two-track approach
- [USER_STORY.md](USER_STORY.md) — Your daily experience as a user
- [DEPLOYMENT_GUIDE.md](DEPLOYMENT_GUIDE.md) — How others will run Refarm once published

---

## Related Documents

- [`roadmaps/MAIN.md`](../roadmaps/MAIN.md) — Project roadmap
- [`docs/REPOSITORY_MIGRATION_GUIDE.md`](REPOSITORY_MIGRATION_GUIDE.md) — Step-by-step org transfer
- [`docs/POST_TRANSFER_CHECKLIST.md`](POST_TRANSFER_CHECKLIST.md) — Post-transfer actions
- [`docs/BRANCH_PROTECTION_SETUP.md`](BRANCH_PROTECTION_SETUP.md) — Branch protection
- [`specs/ADRs/ADR-048-tractor-graduation.md`](../specs/ADRs/ADR-048-tractor-graduation.md) — Tractor graduation
- [`packages/tractor/docs/ARCHITECTURE.md`](../packages/tractor/docs/ARCHITECTURE.md) — Consumer map
