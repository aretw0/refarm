{
  "context": "Checkpoint 2026-07-22 (5th). The no-token runtime e2e smoke now has a CI witness (bc4ed7c6): it rides the build-wasm job in test.yml, gated on run_task_smoke/tractor_gates paths, with a direct pnpm tractor build guaranteeing the release binary against turbo's outputs-less cache hits. This closes the loop the smoke's own rot exposed — drift without a scheduled witness stays invisible. Workflow validators (actions:pins, actions:contracts) pass; the smoke itself was proven locally in c678331f.",
  "timestamp": "2026-07-22T12:46:18.896Z",
  "current_phase": "12",
  "current_tasks": [
    "Commit bc4ed7c6 ci(test): e2e mock smoke wired into build-wasm with path gates and timeout headroom."
  ],
  "blockers": [],
  "next_actions": [
    "Watch the first CI run of the new smoke step on the next push/PR (gh run watch --exit-status) — a CI-only environment difference (ports, HOME, cargo target) would surface there, not locally.",
    "Daily-driver priority 1 continues: exercise runtime up → ask → session → resume → finish as real operator slices; fix what breaks.",
    "Standing tails stay demand-driven: wallet follow-ons (OPFS, WASM signer), ADR-087 allowlist chewing, publication hold behind the daily-driver gate."
  ],
  "open_questions": [],
  "key_decisions_active": [
    "DEC-031: host unificado refarm com renderers TUI/Web/headless sobre contratos compartilhados.",
    "DEC-032: produto CLI/host em apps/; packages/ guardam blocos reutilizáveis.",
    "DEC-034: artefatos WASM Rust devem ser copiados para dist/ durante build.",
    "DEC-035: provision para, configure ainda não existe — silo é o ponto de integração.",
    "DEC-036 [DECIDED]: refarm chat é cliente HTTP do sidecar farmhand em :42001. Não embeda farmhand.",
    "ADR-064 [ACCEPTED]: TokenAuthError com provider + reason + rotationUrl — produzido na camada de credenciais, renderizado por qualquer superfície.",
    "ADR-065 [ACCEPTED]: Farmhand transparent lifecycle — refarm é o single entry point. Auto-start com Y/n confirm, detached spawn, 300ms poll. LaunchDeps interface para testabilidade."
  ],
  "files_in_flux": []
}
