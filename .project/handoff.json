{
  "context": "Checkpoint 2026-07-24 (27th). AUTH — the operator-usable CLI loop is COMPLETE: workspace-access-contract-v1 + opt-in §8 sidecar gate (auth.rs) + farm-client FARM_TOKEN + 'refarm auth enroll/list' (commits 6f3f5bb6, af560a9a, 00343d5f, 31d704f2). Proven end-to-end: enroll a device → daemon with that policy 401s without the token, works with FARM_TOKEN. Daemon runs DEFAULT (gate off) so devices keep working. Operator can turn on CLI auth TODAY: 'refarm auth enroll <id>' → FARM_TOKEN + REFARM_AUTH_POLICY. Public tunnel down; daemon tailnet-only.",
  "timestamp": "2026-07-25T02:26:59.378Z",
  "current_phase": "12",
  "current_tasks": [],
  "blockers": [],
  "next_actions": [
    "AUTH hub+sync pair (FRESH pass — fiddly §8 security, investigated + spec'd): (1) /sync WS gate at daemon::WsServer (main.rs:561) — a browser CAN'T set Authorization on a WS, so use a subprotocol or ?token= or first-frame; the web-serve /sync proxy forwards it. (2) apps/me sends Authorization on /efforts (browser analog of FARM_TOKEN) from a client-side store + minimal login. Only after BOTH is a public tunnel safe for the hub.",
    "AUTH Slice 2: namespace-per-request (gate injects ResolvedWorkspace via resolveAccess; 8 sites mod.rs + 2 dispatch.rs). Slice 3: full isolation (streams/efforts/sync daemon-global).",
    "UX: hybrid home for apps/me (chat-centered + attention strip; strip me-surfaces.ts debug panel; fix i18n). Phone-testable once hub auth lands (safe credential-gated tunnel)."
  ],
  "open_questions": [],
  "key_decisions_active": [
    "DEC-031: host unificado refarm com renderers TUI/Web/headless sobre contratos compartilhados.",
    "DEC-032: produto CLI/host em apps/; packages/ guardam blocos reutilizáveis.",
    "DEC-034: artefatos WASM Rust devem ser copiados para dist/ durante build.",
    "DEC-035: provision para, configure ainda não existe — silo é o ponto de integração.",
    "DEC-036 [DECIDED]: refarm chat é cliente HTTP do sidecar farmhand em :42001. Não embeda farmhand.",
    "ADR-064 [ACCEPTED]: TokenAuthError com provider + reason + rotationUrl — produzido na camada de credenciais, renderizado por qualquer superfície.",
    "ADR-065 [ACCEPTED]: Farmhand transparent lifecycle — refarm é o single entry point. Auto-start com Y/n confirm, detached spawn, 300ms poll. LaunchDeps interface para testabilidade."
  ],
  "files_in_flux": []
}
