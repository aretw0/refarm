{
  "context": "Checkpoint 2026-07-22 (12th). Phone probe data proved both devices share the /24 — the router filters broadcast AND multicast between clients. Dialect 3 landed (3a35350a): bounded unicast sweep of the /24 (refuses wider subnets), staged after the cheap dialects, with farm-name dedup and own-subnet address preference. Operator raised the DISTRIBUTION question: stop running ad-hoc scripts on the phone, build refarm ON THE PC and hand the device a finished artifact — the script only bootstraps and fetches.",
  "timestamp": "2026-07-22T20:50:22.648Z",
  "current_phase": "12",
  "current_tasks": [
    "Commits 25357674 (multicast + Pears spec), 3a35350a (unicast sweep) — pushed pending; beacon suite 6 green."
  ],
  "blockers": [],
  "next_actions": [
    "RETRY on the phone after git pull: plain farm-hello — the sweep should find the farm if client unicast passes; if even that fails, AP isolation is total and the P2P rail (Pears spec fatia 2) becomes the only LAN-independent path.",
    "Device distribution channel (operator direction): rung 1 git-pull (current); rung 2 single-file device CLI bundled ON the PC (esbuild-class bundler as devDep, apps/refarm dist -> one .mjs; verify no native deps in the CLI graph), delivered via GitHub Release asset — never committed dist (source sovereignty); farm-hello gains a fetch-once bootstrap. Rung 3: device CLI pointed at the farm through the governed tunnel, not raw sidecar exposure. Rung 4: bare/Pears runtime as the true mobile citizen (spec fatia 1 research).",
    "Feedback loop com o Android é o laboratório: cada limitação encontrada (broadcast filtrado, multicast filtrado, contexto seguro, agora distribuição) virou primitiva ou spec no mesmo dia — manter esse ritmo."
  ],
  "open_questions": [],
  "key_decisions_active": [
    "DEC-031: host unificado refarm com renderers TUI/Web/headless sobre contratos compartilhados.",
    "DEC-032: produto CLI/host em apps/; packages/ guardam blocos reutilizáveis.",
    "DEC-034: artefatos WASM Rust devem ser copiados para dist/ durante build.",
    "DEC-035: provision para, configure ainda não existe — silo é o ponto de integração.",
    "DEC-036 [DECIDED]: refarm chat é cliente HTTP do sidecar farmhand em :42001. Não embeda farmhand.",
    "ADR-064 [ACCEPTED]: TokenAuthError com provider + reason + rotationUrl — produzido na camada de credenciais, renderizado por qualquer superfície.",
    "ADR-065 [ACCEPTED]: Farmhand transparent lifecycle — refarm é o single entry point. Auto-start com Y/n confirm, detached spawn, 300ms poll. LaunchDeps interface para testabilidade."
  ],
  "files_in_flux": []
}
