{
  "context": "Checkpoint 2026-07-22 (10th). The phone test became a TEST: smoke:remote-device drives real Chromium at the host's LAN https origin against the built hub — and its first run caught three browser-graph regressions (personal panel's node deps, wallet mounted via the node barrel instead of @refarm.dev/wallet/browser, chat-composer importing the config root), all fixed (8f8d2b67); the smoke now passes the full outward contract (081a6f41). farm-hello.mjs opens the Termux/Raspberry door: zero-dep device probe via git pull. Known issue: the dev-mode smoke's managed astro server accumulates stale daemons and vite first-load re-optimization churn — needs its own lifecycle slice; the built-dist smoke is authoritative.",
  "timestamp": "2026-07-22T19:18:48.240Z",
  "current_phase": "12",
  "current_tasks": [
    "Commits 8f8d2b67, 081a6f41 — remote-device smoke green end-to-end; farm-hello green against the live daemon."
  ],
  "blockers": [],
  "next_actions": [
    "LIVE with the operator: on the phone (Termux: pkg install git nodejs), git clone/pull develop, node scripts/farm-hello.mjs <host-ip>. WS probe should pass already; the sidecar probe needs the daemon started with --http-host 0.0.0.0. Then the browser milestone via smoke:remote-device's manual twin.",
    "Pears/Holepunch assimilation spec (operator direction): P2P transport as a seam for sync (hyperswarm-style hole punching vs LAN ws), Pear as a distribution channel — write the spec in docs/superpowers/specs before any code, agents-lab-portability style.",
    "Raspberry Pi lane: Node path first (farm-hello → farmhand TS engine on ARM), Rust tractor aarch64 later; same git-pull distribution as Termux.",
    "Dev-mode smoke lifecycle slice: stale astro daemons + vite optimize churn made the dev harness flaky; the reload retry is in, the server lifecycle needs ownership."
  ],
  "open_questions": [],
  "key_decisions_active": [
    "DEC-031: host unificado refarm com renderers TUI/Web/headless sobre contratos compartilhados.",
    "DEC-032: produto CLI/host em apps/; packages/ guardam blocos reutilizáveis.",
    "DEC-034: artefatos WASM Rust devem ser copiados para dist/ durante build.",
    "DEC-035: provision para, configure ainda não existe — silo é o ponto de integração.",
    "DEC-036 [DECIDED]: refarm chat é cliente HTTP do sidecar farmhand em :42001. Não embeda farmhand.",
    "ADR-064 [ACCEPTED]: TokenAuthError com provider + reason + rotationUrl — produzido na camada de credenciais, renderizado por qualquer superfície.",
    "ADR-065 [ACCEPTED]: Farmhand transparent lifecycle — refarm é o single entry point. Auto-start com Y/n confirm, detached spawn, 300ms poll. LaunchDeps interface para testabilidade."
  ],
  "files_in_flux": []
}
