{
  "context": "Checkpoint 2026-07-24 (24th). SECURITY design + first block. Spec written (docs/superpowers/specs/2026-07-24-sovereign-auth-workspaces-design.md): daemon has NO app-auth today (only Tailscale); layered model network→app-auth-gate→identity-workspace (personal isolated + shared collective). Operator constraint: GENERIC BLOCKS IN PACKAGES ALWAYS, never inline-per-use. First block landed: @refarm.dev/workspace-access-contract-v1 (workspace-access:v1) — pure resolveAccess(policy, identity, requestedWorkspace)→{namespace}|{denied} + validatePolicy + workspacesFor; 10 tests; the reusable authorization heart the §8 gate enforces and the hub/enrollment read. Commit 6f3f5bb6. ALSO this session: chat-over-tunnel fixed (web serve proxies sidecar HTTP /efforts, commit 960c150d), cloudflared quick-tunnel PWA install proven (then torn down — public+unauth risk), PWA audit (apps/me already has manifest+SW+registration+smoke). Public tunnel is DOWN; daemon tailnet-only.",
  "timestamp": "2026-07-25T00:27:44.484Z",
  "current_phase": "12",
  "current_tasks": [],
  "blockers": [],
  "next_actions": [
    "AUTH Slice 1 — the §8 daemon gate (packages/tractor): axum from_fn layer at sidecar mod.rs:1573 (mirror CORS) + /sync WS: extract per-device credential → identity (identity block / deriveFromSession) → resolveAccess (the new contract) → 401 (TokenAuthError vocab) or inject ResolvedWorkspace. Provision the host's own credential via silo; farm-client + apps/me + web-serve proxy forward it. Ship gate + client credential together (breaking change). Do it in a FRESH pass — security §8, not the tail of a long session.",
    "AUTH Slice 2 — namespace-per-request: 8 NativeStorage::open sites in mod.rs + 2 in dispatch.rs read ResolvedWorkspace.namespace; state.namespace demotes to default. Slice 3 (larger axis) = full isolation of streams/efforts/plugins/sync (daemon-global today).",
    "UX Slice — the hybrid home for apps/me (chat-centered + slim attention strip): strip the debug surfaces (me-surfaces.ts), fix i18n, move status/wallet/graph behind a tap. Mockup aligned with operator. (Chat now works over the tunnel.)"
  ],
  "open_questions": [],
  "key_decisions_active": [
    "DEC-031: host unificado refarm com renderers TUI/Web/headless sobre contratos compartilhados.",
    "DEC-032: produto CLI/host em apps/; packages/ guardam blocos reutilizáveis.",
    "DEC-034: artefatos WASM Rust devem ser copiados para dist/ durante build.",
    "DEC-035: provision para, configure ainda não existe — silo é o ponto de integração.",
    "DEC-036 [DECIDED]: refarm chat é cliente HTTP do sidecar farmhand em :42001. Não embeda farmhand.",
    "ADR-064 [ACCEPTED]: TokenAuthError com provider + reason + rotationUrl — produzido na camada de credenciais, renderizado por qualquer superfície.",
    "ADR-065 [ACCEPTED]: Farmhand transparent lifecycle — refarm é o single entry point. Auto-start com Y/n confirm, detached spawn, 300ms poll. LaunchDeps interface para testabilidade."
  ],
  "files_in_flux": []
}
