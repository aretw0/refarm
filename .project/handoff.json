{
  "context": "Checkpoint 2026-07-22 (19th). The device kit is now a REAL PACKAGE: @refarm.dev/farm-client (js-tool, zero runtime deps, pure ESM). The operator's intent — decentralize what was stuck in refarm so it/others receive via import, not just ad-hoc scripts. src/ is the importable library (beacon/tailnet/effort-result, 16 exports + subpaths); bin/ are the git-pull executables (farm-hello/announce/ask, import ../src relatively so no install needed on Termux). Decoupling is an in-package test invariant. apps/refarm discover spawns the package bin; scripts/farm-*.mjs are thin compat shims. Answer to the operator's question: it is a PACKAGE not an app (apps are opinionated distros; this is a reusable block). Proven live over the tailnet + as a library import. All structural validators green (validate-packages js-tool, test-runner:contracts, brand guard).",
  "timestamp": "2026-07-22T23:28:33.324Z",
  "current_phase": "12",
  "current_tasks": [
    "Commit (farm-client package) — device kit promoted; 18 pkg tests + validators green; farm-ask proven live from the package bin."
  ],
  "blockers": [],
  "next_actions": [
    "Promotion follow-through when ready: consider farm-client for the vault-seed-ready selection (product-neutral consumer-pulled block) once an external consumer imports it; add it to DISTRIBUTION_STATUS if selected.",
    "Depth options unchanged, all decoupled: HUB in the phone browser (refarm web serve + TLS + tailnet name); distribution rung 2 (bundle apps/refarm CLI itself, separate from farm-client); sovereign follow-ons (daemon dual-bind, Cloudflare tunnel, Pears P2P).",
    "Keep the workload-neutral framing: agent is one plugin verb; farm-client's effort-result and wire contract are workload-neutral."
  ],
  "open_questions": [],
  "key_decisions_active": [
    "DEC-031: host unificado refarm com renderers TUI/Web/headless sobre contratos compartilhados.",
    "DEC-032: produto CLI/host em apps/; packages/ guardam blocos reutilizáveis.",
    "DEC-034: artefatos WASM Rust devem ser copiados para dist/ durante build.",
    "DEC-035: provision para, configure ainda não existe — silo é o ponto de integração.",
    "DEC-036 [DECIDED]: refarm chat é cliente HTTP do sidecar farmhand em :42001. Não embeda farmhand.",
    "ADR-064 [ACCEPTED]: TokenAuthError com provider + reason + rotationUrl — produzido na camada de credenciais, renderizado por qualquer superfície.",
    "ADR-065 [ACCEPTED]: Farmhand transparent lifecycle — refarm é o single entry point. Auto-start com Y/n confirm, detached spawn, 300ms poll. LaunchDeps interface para testabilidade."
  ],
  "files_in_flux": []
}
