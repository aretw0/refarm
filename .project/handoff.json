{
  "context": "Checkpoint 2026-07-24 (26th). AUTH — the CLI loop is CLOSED end-to-end. Three pieces landed: (1) workspace-access-contract-v1 (authorization block, 6f3f5bb6); (2) the opt-in §8 gate on the sidecar (auth.rs, af560a9a) — REFARM_AUTH_POLICY set → 401 without a valid Bearer token; (3) farm-client carries the credential (FARM_TOKEN → Authorization: Bearer, 00343d5f). PROVEN live: gate on → farm-ask blocked without FARM_TOKEN, works with the enrolled token; gate off (default) → unchanged. So a public tunnel is now SAFELY usable when the gate is on (credential-gated). Daemon runs DEFAULT (gate off) so devices keep working; test policy at scratchpad/auth-policy.json (token 'test-device-token' → id-test). Full arc this session: usage footer→prompt caching(84%)→mesh distribution→cold-bootstrap→kit-remembers→progress spinner→plan-gated error hint→chat-over-tunnel fix→PWA audit→sovereign-auth (spec + contract + gate + client).",
  "timestamp": "2026-07-25T01:11:46.158Z",
  "current_phase": "12",
  "current_tasks": [],
  "blockers": [],
  "next_actions": [
    "AUTH finish: (a) the browser hub credential — apps/me sends Authorization via the same-origin proxy (proxy already forwards headers); needs a client-side credential store / minimal login. (b) gate /sync (CRDT WS :42000, separate code path in main.rs — currently ungated). (c) an enroll command (refarm auth enroll <identity> → mint token + write policy) for ergonomics. After these, the operator can flip REFARM_AUTH_POLICY on across all surfaces.",
    "AUTH Slice 2: namespace-per-request (gate injects ResolvedWorkspace via workspace-access resolveAccess; 8 sites mod.rs + 2 dispatch.rs). Slice 3: full isolation (streams/efforts/sync daemon-global today).",
    "UX: the hybrid home for apps/me (chat-centered + attention strip; strip me-surfaces.ts debug panel; fix i18n). Now SAFELY testable on the phone via a credential-gated tunnel (the reason auth came first)."
  ],
  "open_questions": [],
  "key_decisions_active": [
    "DEC-031: host unificado refarm com renderers TUI/Web/headless sobre contratos compartilhados.",
    "DEC-032: produto CLI/host em apps/; packages/ guardam blocos reutilizáveis.",
    "DEC-034: artefatos WASM Rust devem ser copiados para dist/ durante build.",
    "DEC-035: provision para, configure ainda não existe — silo é o ponto de integração.",
    "DEC-036 [DECIDED]: refarm chat é cliente HTTP do sidecar farmhand em :42001. Não embeda farmhand.",
    "ADR-064 [ACCEPTED]: TokenAuthError com provider + reason + rotationUrl — produzido na camada de credenciais, renderizado por qualquer superfície.",
    "ADR-065 [ACCEPTED]: Farmhand transparent lifecycle — refarm é o single entry point. Auto-start com Y/n confirm, detached spawn, 300ms poll. LaunchDeps interface para testabilidade."
  ],
  "files_in_flux": []
}
