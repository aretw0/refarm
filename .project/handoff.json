{
  "context": "Checkpoint 2026-07-25 (28th). Session close with DURABLE MATERIAL left for a rested-operator fresh pass (his instruction: advance where safe, leave specs/features/ADRs for the rest). DONE + proven this session: CLI auth loop complete (workspace-access-contract-v1 + opt-in §8 sidecar gate auth.rs + farm-client FARM_TOKEN + 'refarm auth enroll/list'); operator can turn on CLI auth today. LEFT AS DESIGN (ready to implement): (a) ADR-093 (specs/ADRs/) — the browser-hub + /sync WS credential channel decision (hub HTTP via stored token bootstrapped by one-time ?token=; /sync via Sec-WebSocket-Protocol subprotocol since a browser can't set Authorization on a WS; validated at daemon::WsServer handshake, fail-closed, one shared policy). (b) docs/superpowers/specs/2026-07-25-hub-hybrid-home-design.md — the chat-centered hybrid home for apps/me. Daemon runs DEFAULT (gate off). Real policy at .refarm/auth-policy.json is gitignored.",
  "timestamp": "2026-07-25T12:20:59.768Z",
  "current_phase": "12",
  "current_tasks": [],
  "blockers": [],
  "next_actions": [
    "AUTH hub+sync (implement ADR-093, §8+apps/me): /sync WS gate at daemon::WsServer via Sec-WebSocket-Protocol subprotocol (reuse sidecar::auth::AuthPolicy::authenticate) + web-serve /sync proxy forwards the header + apps/me sends Authorization on /efforts and the subprotocol on the sync socket, token from localStorage bootstrapped by ?token=. Only then is a public tunnel safe for the hub.",
    "UX hybrid home (implement the 2026-07-25 design doc, apps/me, non-§8): chat-centered + attention strip; strip me-surfaces debug panel; fix i18n. Build layout locally, validate on phone once ADR-093 lands.",
    "AUTH Slice 2 (namespace-per-request via resolveAccess; 8 sites mod.rs + 2 dispatch.rs) then Slice 3 (full isolation: streams/efforts/sync are daemon-global). All in docs/superpowers/specs/2026-07-24-sovereign-auth-workspaces-design.md."
  ],
  "open_questions": [],
  "key_decisions_active": [
    "DEC-031: host unificado refarm com renderers TUI/Web/headless sobre contratos compartilhados.",
    "DEC-032: produto CLI/host em apps/; packages/ guardam blocos reutilizáveis.",
    "DEC-034: artefatos WASM Rust devem ser copiados para dist/ durante build.",
    "DEC-035: provision para, configure ainda não existe — silo é o ponto de integração.",
    "DEC-036 [DECIDED]: refarm chat é cliente HTTP do sidecar farmhand em :42001. Não embeda farmhand.",
    "ADR-064 [ACCEPTED]: TokenAuthError com provider + reason + rotationUrl — produzido na camada de credenciais, renderizado por qualquer superfície.",
    "ADR-065 [ACCEPTED]: Farmhand transparent lifecycle — refarm é o single entry point. Auto-start com Y/n confirm, detached spawn, 300ms poll. LaunchDeps interface para testabilidade."
  ],
  "files_in_flux": []
}
