{
  "context": "Checkpoint 2026-07-22 (7th) — STRATEGIC DIRECTION CHANGE by operator decision (Arthur): the handoff is not a dictator; the outward horizon (refarm operating through the operator's devices and platforms, individual and collective) outranks further inward daily-driver hardening. A reach map was compiled: the CRDT sync socket already binds 0.0.0.0:42000; the gaps were pointing and serving, not engineering. Two landed today: the hub derives/accepts its sync URL (c48e7033 — multi-device sync unblocked on the pointing side) and refarm serve gained --host (a034071a). The daily-driver loop remains legitimate where it unblocks reach.",
  "timestamp": "2026-07-22T16:04:36.700Z",
  "current_phase": "12",
  "current_tasks": [
    "Commits c48e7033 (hub sync URL seam: env-validated + location-derived), a034071a (serve --host bind seam), 147a3745 (contract fixture hoist). All lanes green."
  ],
  "blockers": [],
  "next_actions": [
    "Outward slice 3: farmhand HTTP sidecar bind parity with the Rust daemon (apps/farmhand/src/transports/http.ts hardcodes 127.0.0.1; read FARMHAND_HTTP_HOST like the daemon's --http-host).",
    "Outward slice 4: a real 'refarm web serve <dir> --host' static server for apps/me/dist with COOP/COEP headers — the missing hub-serve command (spec already suggested in local-surface).",
    "Outward slice 5 (the true wall): secure-context path for LAN — https/mkcert or tunnel so service worker + crossOriginIsolated (OPFS/WASM boot) work off-localhost. Then: hands-on proof from a real second device.",
    "Interop lanes mapped for later: Pi->refarm effort submission already works via sidecar POST /efforts; channel contracts (Telegram et al) are consumer-owned in vault-seed; agents-lab needs the farmhand skill engine-binding."
  ],
  "open_questions": [],
  "key_decisions_active": [
    "DEC-031: host unificado refarm com renderers TUI/Web/headless sobre contratos compartilhados.",
    "DEC-032: produto CLI/host em apps/; packages/ guardam blocos reutilizáveis.",
    "DEC-034: artefatos WASM Rust devem ser copiados para dist/ durante build.",
    "DEC-035: provision para, configure ainda não existe — silo é o ponto de integração.",
    "DEC-036 [DECIDED]: refarm chat é cliente HTTP do sidecar farmhand em :42001. Não embeda farmhand.",
    "ADR-064 [ACCEPTED]: TokenAuthError com provider + reason + rotationUrl — produzido na camada de credenciais, renderizado por qualquer superfície.",
    "ADR-065 [ACCEPTED]: Farmhand transparent lifecycle — refarm é o single entry point. Auto-start com Y/n confirm, detached spawn, 300ms poll. LaunchDeps interface para testabilidade."
  ],
  "files_in_flux": []
}
