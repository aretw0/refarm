{
  "context": "Checkpoint 2026-07-22 (4th). Daily-driver front: the no-token execution-plane gate (refarm:agent:e2e:mock) passes end-to-end on this host with the Rust engine for the first time. Running it surfaced two silent drifts in the smoke itself, both fixed (c678331f): it booted the raw wasm without the manifest the responder election reads (now materializes plugin.wasm+plugin.json like load_plugin_by_hash), and its asserts predated the urn:sovereign URN migration (also fixed in the tree smoke stubs). ADR-087 orbit closed per operator direction: the ratchet guard automates vigilance; no more standalone brand work.",
  "timestamp": "2026-07-22T11:36:02.796Z",
  "current_phase": "12",
  "current_tasks": [
    "Commit c678331f fix(ci): e2e mock smoke materializes the plugin pair + URN asserts updated.",
    "Validation: pnpm run refarm:agent:e2e:mock exit 0 (plugin elected, mock ask, task dispatch, 2 stream files, resume handoffs); smoke-refarm-tree-cli passed; after-edit/after-commit lanes ok."
  ],
  "blockers": [],
  "next_actions": [
    "Daily-driver priority 1 continues: keep exercising runtime up → ask → session → resume → finish as real operator slices and fix what breaks; the readiness doc records both validations (devcontainer 06-13, host 07-22).",
    "Consider wiring the e2e mock smoke (or a cheap subset) into CI — its two drifts stayed invisible precisely because no workflow runs it.",
    "Wallet follow-ons stay demand-driven (OPFS store, WASM signer); brand tail stays governed in ADR-087."
  ],
  "open_questions": [],
  "key_decisions_active": [
    "DEC-031: host unificado refarm com renderers TUI/Web/headless sobre contratos compartilhados.",
    "DEC-032: produto CLI/host em apps/; packages/ guardam blocos reutilizáveis.",
    "DEC-034: artefatos WASM Rust devem ser copiados para dist/ durante build.",
    "DEC-035: provision para, configure ainda não existe — silo é o ponto de integração.",
    "DEC-036 [DECIDED]: refarm chat é cliente HTTP do sidecar farmhand em :42001. Não embeda farmhand.",
    "ADR-064 [ACCEPTED]: TokenAuthError com provider + reason + rotationUrl — produzido na camada de credenciais, renderizado por qualquer superfície.",
    "ADR-065 [ACCEPTED]: Farmhand transparent lifecycle — refarm é o single entry point. Auto-start com Y/n confirm, detached spawn, 300ms poll. LaunchDeps interface para testabilidade."
  ],
  "files_in_flux": []
}
