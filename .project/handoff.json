{
  "context": "Checkpoint 2026-07-22 (15th). Tailscale UP on host: mesh IP 100.105.71.127, MagicDNS name serpro-1577853.tail894688.ts.net. PROVEN LIVE: sync CRDT answers over BOTH the mesh IP and the MagicDNS name (daemon binds 0.0.0.0, so the tailnet address is covered with zero change). Convergence answer to the operator: on a tailnet the phone uses the NAME, not an IP — farm-hello <name> resolves via MagicDNS from any network. farm-hello gained dialect 0 (tailnet peer enumeration via tailscale status --json, probes each peer's sync directly, no announcer needed); 0 peers today because the phone has not joined. Announcer religado governed on host.",
  "timestamp": "2026-07-22T22:04:29.800Z",
  "current_phase": "12",
  "current_tasks": [
    "Commit 9672750b — tailnet discovery + MagicDNS handle; tailnet:test 4 green; sync proven over mesh IP and name."
  ],
  "blockers": [],
  "next_actions": [
    "OPERATOR: join the phone to the tailnet (Tailscale Android app, same account aretw0@). Then on Termux: node scripts/farm-hello.mjs serpro-1577853 — NO IP, from mobile data (Wi-Fi off) — the first cross-device crossing. sync should be ✅ over the mesh; sidecar ❌ until sidecar exposure is decided (separate).",
    "Once the phone is a peer, farm-hello with NO argument should auto-find it via tailnet peer enumeration — the true zero-typing path. Verify.",
    "Then distribution rung 2 (bundle apps/refarm CLI to single .mjs, Release asset); Cloudflare tunnel for the public hub; Pears spec fatia 1 research. Optional: farm-announce could bridge the beacon over the tailnet, or a refarm net status reading tailscale status."
  ],
  "open_questions": [],
  "key_decisions_active": [
    "DEC-031: host unificado refarm com renderers TUI/Web/headless sobre contratos compartilhados.",
    "DEC-032: produto CLI/host em apps/; packages/ guardam blocos reutilizáveis.",
    "DEC-034: artefatos WASM Rust devem ser copiados para dist/ durante build.",
    "DEC-035: provision para, configure ainda não existe — silo é o ponto de integração.",
    "DEC-036 [DECIDED]: refarm chat é cliente HTTP do sidecar farmhand em :42001. Não embeda farmhand.",
    "ADR-064 [ACCEPTED]: TokenAuthError com provider + reason + rotationUrl — produzido na camada de credenciais, renderizado por qualquer superfície.",
    "ADR-065 [ACCEPTED]: Farmhand transparent lifecycle — refarm é o single entry point. Auto-start com Y/n confirm, detached spawn, 300ms poll. LaunchDeps interface para testabilidade."
  ],
  "files_in_flux": []
}
