{
	"context": "Checkpoint 2026-08-06. Three things now hold that did not before, each proven on node `sede` rather than argued. The cost axis is populated at the origin (refarm.workspace.id 2/23 -> 8/29). Refarm can SAY which sovereign state is active, and its divergences reach doctor and check. And the record reader stopped lying about which records it returns: `refarm budget observations --limit 1` returned the OLDEST of 29 and now returns the newest. TWO of the three operational defects from the previous checkpoint are FIXED; the third is open and named.",
	"timestamp": "2026-08-06T15:49:29.025112Z",
	"current_phase": "14",
	"current_tasks": [
		"SHIPPED - the guest can tell, and the budget guard was switched back on. `query-nodes` now returns a node-page record (nodes, stored, truncated) instead of a bare list -- a BREAKING change to plugin:host@0.1.0, chosen over an additive second function because that would leave a shorter-named blind alternative alive. Eight call sites in agent and delegate updated; four crates that import the interface and never call it regenerated for free. Proven live: hashes moved together cff89975 -> 50483c6c, harness 3/3 against the NEW contract, predecessors unregressed. THE REAL FIND: budget_exceeded_for_provider was effectively disabled. It summed 30 days of UsageRecord spend, and a truncated read summed a partial set while a query error became zero spend -- both resolving to 'not over budget'. Before the ordering fix it took the OLDEST 10,000 rows, which against a 30-day window are mostly outside it. Nobody had connected an ordering bug to budget enforcement. Three cases that looked like policy turned out to be arithmetic: a zero ceiling blocks before any query (any non-negative spend meets it); a partial sum already at budget blocks (a lower bound above the ceiling proves the total is above it); truncated-and-still-under re-queries with limit=stored and decides on the complete set. Only a genuine query error remains Unknown. The `Truncated` variant was DELETED rather than left unreachable. Verified case by case: no block the operator had is lost, and one he never had is gained.",
		"SHIPPED - the ordering contract reaches every consumer that was holding the old shape. The host bridge stopped materialising whole tables (it now calls query_nodes_limited, with a NativeSync passthrough added); packages/sidecar-client's queryNodes stopped discarding stored/truncated, with absent genuinely staying absent; and BOTH agent session helpers stopped re-sorting with max_by_key(created_at_ns) over rows already ordered by updated_at. Their decisions were extracted into session::pure so they could be tested natively despite mod wasm_ops being cfg(wasm32) -- honest coverage where the plan had authorised its absence. Proven live: component rebuilt with loaded and built plugin hashes moving TOGETHER from 544ef5b4 to cff89975, harness session 3/3, sessions resolving, and the predecessor's fix unregressed.",
		"RECORDED - SIX instruments were found in this line of work reporting results they had not earned, and the sixth was introduced by the fix for the fifth. In order: refarm context printed 'No divergences' when run from outside the repo; the runtime freshness check fell back to a guessed path and reported fresh; budget.ts invented truncated: false when the server said nothing; the ordering tests passed with the primary sort key deleted, because every row was written inside one second so only the tiebreak decided; `pnpm run diagrams:check` ran without --ci and could not fail by construction; and the fix for that baked REFARM_DIAGRAM_SYNC_STRICT=0 into the npm script, which overrides any caller and made the check permanently unable to fail -- the exact thing that fix's own report had warned against one round earlier. Every instance is the same shape: two states where three were needed, with 'I do not know' collapsing into 'fine'. The antidote that worked each time was structural rather than disciplinary. Written up in docs/SOVEREIGN_RECORD_ORDERING.md and specs/diagrams/DESIGN_SYSTEM.md.",
		"SHIPPED - OPERATIONAL DEFECT 1 IS FIXED. storage/sqlite.rs's query_nodes had no ORDER BY at all, so it returned insertion order while NINE readers took from the front, every one meaning 'the most recent N'. Measured before: `refarm budget observations --limit 1 --json` returned the observation from 2026-08-03T21:21:48, the OLDEST of 29, while the newest was 2026-08-05T17:29:36. Now: ORDER BY updated_at DESC, id DESC, a query_nodes_limited applying the limit IN SQL, a count_nodes for the true total, and `stored`/`truncated` on the response -- genuinely ABSENT when an older node does not report them, never an invented `false`. Proven live: --limit 1 returns 2026-08-05T17:29:36 with stored: 29, truncated: true. The tiebreak is load bearing rather than tidy: updated_at has second resolution, and whether a same-second tie is deterministic depends on how the id was minted -- agent/src/utils.rs mints monotonically, while streaming/observations.rs and wasi_bridge/model_stream_events.rs use Uuid::new_v4(). That difference made one test flake three runs in five and is now documented.",
		"SHIPPED - docs/SOVEREIGN_RECORD_ORDERING.md and specs/diagrams/record-read-path.mermaid, at the operator's explicit request: something this hard to find must be written down once it is right or it gets researched from zero. The document records the measurement, the nine affected readers, the monotonic-versus-random id distinction with its real conditions, what a caller must do, and a named failure shape observed FOUR times in one session across four different authors -- a fix for a silent false-clean containing one, because only two states existed where three were needed and 'I do not know' collapsed into 'fine'. The antidote that worked every time was structural, not disciplinary: make the third state a variant the compiler forces you to handle.",
		"SHIPPED - Refarm can say which sovereign state is active. `refarm context` reports mode, sovereign home and how it was chosen, base, namespace, runtime endpoint, node identity, and the loaded plugin WITH ITS SHA-256, plus a list of divergences that also reach `refarm doctor` and `refarm check`. THE HASH IS THE LOAD-BEARING PART: a path proves nothing, and on this machine four paths held three different binaries. The witness is the running process's own `--plugin` argument read from /proc, never a reconstructed guess -- a reconstruction drifts again at the next installer change, argv does not. Six divergence kinds, with a compile-time exhaustiveness guard so a seventh cannot be silently forgotten, which is exactly how one slipped through mid-plan.",
		"SHIPPED - OPERATIONAL DEFECT 2 IS FIXED, and the root cause was exact: runtime-freshness.ts reconstructed the agent plugin path as `plugins/@refarm/agent/plugin.wasm` while the daemon is started with `plugins/refarm_agent/plugin.wasm`. Both files existed in the same sovereign dir, written the same day, 477,924 bytes against 476,441. The check was sound and pointed at a file nothing loads. This is blocker #8 returning through another door: commits 3a8aeafb and ab573e10 converged the INSTALLER on refarm_agent/ and nobody moved the WATCHER. Proven by deliberately staling the live artifact with `touch` -- changing the mtime without altering a byte -- confirming `runtime:stale` fires naming the loaded path, then restoring the exact mtime with `touch -d`. Restoring by copy was rejected during planning because it would have left the node reporting stale permanently: the proof would have created the condition it was testing for.",
		"SHIPPED - the run says which workspace it is for. A four-degree ladder in `refarm ask`: explicit --workspace (recorded `declared`) > the session's own stored declaration (inherited) > a cwd seed at session creation (recorded `seeded-from-cwd`) > nothing. One resolved value travels twice by design -- Effort.workspaceId to the sidecar for refarm.workspace.id, args.workspace_id to the agent, which stamps the Session node so later runs inherit. cwd is ABSENT from the dispatch-time order (ADR-094 D2) and acts once, at session birth, writing a declaration stamped with its provenance (H2). A read failure of the stored declaration is `unknown`, NOT `unattributed` -- collapsing those two would silently re-attribute an established conversation from wherever the operator happens to stand. LIVE: seed -> `refarm`, declaration -> `rcdc5`, and a flag-less follow-up from a directory seeding `refarm` still recorded `rcdc5`. Coverage 2/23 -> 8/29.",
		"SHIPPED - the declaration reaches the path a real ask takes. The first live proof FAILED on inheritance and that failure was the point: reported honestly, it exposed TWO broken links, which is why every function looked correct in isolation. (1) A `refarm ask` never reaches execute_respond -- the sidecar fires the prompt as a reply-less EventEnvelope::fire(\"user:prompt\"), so the runner takes call_on_event -> handle_prompt, and the EnvGuards sat on a real but different path. (2) Upstream, dispatch.rs::extract_task_args had no workspace_id field at all, so the payload never carried the declaration. The regression test drives the real user:prompt path against the real agent.wasm and was PROVEN to fail against the pre-fix binary; clean_model_env gained both vars so a leaked environment cannot forge a pass.",
		"SHIPPED - the record can say a run was WRONG. refarm.verification.expected/.passed/.method/.unknown, with refarm.outcome untouched: `done` still means the run completed, and correctness is a separate fact. Three states are STRUCTURAL (Matched | NotMatched | Unverifiable(reason), no Absent variant), so an expectation declared but not comparable can never be recorded as a failure. `refarm ask --expect <text>`, substring match, method recorded beside the verdict. Proven live: expect 59 -> passed, expect 9999 -> failed, outcome `done` in both.",
		"SHIPPED - the step fraction, and the 4/25 mystery is solved. steps_completed/steps_planned now count the SAME notion (completion-loop steps) and match the `step N/M` the sidecar prints; the turn count survives as refarm.outcome.turns_completed and travels ALONE, never as half a fraction. Why it looked impossible: a turn is a DISPATCH (one refarm ask = one turn) and nothing declares a max number of turns, so a turn count genuinely has no denominator -- the observation concluded, reasonably and wrongly, that only an AgentPlan could supply one. The 25 was never a plan; it is the react loop's own ceiling, and it always rode on prompt_ref, the same key the observation joins on. A second real off-by-one was found underneath: the loop ran 0..=max_iter, so a ceiling of 25 made 26 completions and would have recorded 26/25. BEHAVIOUR CHANGE: MODEL_TOOL_CALL_MAX_ITER=N now makes N completions per dispatch, not N+1.",
		"SHIPPED - work is comparable. refarm.scenario.id (declared, never invented) and .hash (derived from the request shape at dispatch, canonicalised, key-order-stable). Two exclusions were found by MEASURING rather than reasoning: args.replyRef and args.session_id are per-run identifiers, and leaving either in made every run hash uniquely. A fact that is NOT a defect and must not be 'fixed': an agent request is never identical to itself, because ask's system prompt is assembled with DateContextProvider and GitStatusContextProvider. So grouping on the agent path comes from the DECLARED id -- `refarm ask --scenario X` -- not from the hash.",
		"SHIPPED - the node can be restarted safely and says when it is stale. SIGTERM handled like SIGINT (it was not, so every scheduled restart would have been a hard kill), GET /efforts/in-flight, and a drain bounded by the longest in-flight deadline the budget already resolved. Plus two doctor findings: runtime:stale (the running node predates artifacts on disk -- caught the operator's node nine hours behind its own binary while `refarm check` said all-clear) and runtime:bare-environment (a node started without .refarm/.env falls to the keyless local floor and fails only at first use).",
		"SHIPPED - the catalog reaches the node and leaves the crate. 28 verified entries with per-fact provenance, a shadowing guard proven by sabotage, an `unpriced` third state, a managed-file materialiser that updates an untouched copy and KEEPS an edited one, and a crate_boundary test that fails when a production include_str! escapes the crate. Note refarm-tractor is STILL unpublishable for a second reason found by that test: bindgen! reads ../plugin-wit/wit. Recorded, not fixed, in the router spec's D1.",
		"OPEN - cost separation is RECORDED but not QUERYABLE. Every observation already carries host.name, host.id, refarm.workspace.id, refarm.budget.spawner and refarm.cost.estimated_usd -- who ran it, in which workspace, from which surface, for how much. `refarm budget` has exactly ONE subcommand, `observations`. There is no way to ask what rcdc5 cost this month, or what a node spent, or which surface. The operator named cost audit and separation on 2026-08-05 and it is otherwise unmapped.",
		"OPEN - the fourth budget axis, and it governs what the operator ACTUALLY runs. His daily route is openai-codex, a SUBSCRIPTION, so refarm.cost.estimated_usd is honestly 0 and every dollar ceiling measures nothing for the work he does most. The binding constraint is requests per billing period, a quota that REFILLS across runs rather than a ceiling consumed within one. Already specified as a gap in the budget-laboratory design (lines 460-461). Good news measured 2026-08-05: the axis needs no new counter -- observations already carry timestamp_ns and refarm.pricing_mode, so a period's usage is countable from the record the laboratory already keeps.",
		"OPEN - two installers wrote the agent to two directories. `refarm plugin install` writes plugins/refarm_agent/; tractor-start.sh loads plugins/@refarm/agent/ via scripts/agent-install.mjs. The daemon loads the second, so the command the CLI RECOMMENDS on a plugin failure cannot fix it. A fix was in flight at checkpoint time; verify it landed before trusting `refarm plugin install`."
	],
	"blockers": [
		"OPERATIONAL DEFECT 3 -- `cargo component build --release` does NOT republish packages/agent/dist/agent.wasm, which is what `refarm plugin install` reads. The documented build command silently leaves stale code installed. Working path: `pnpm --filter @refarm.dev/agent run build` -> `refarm plugin install` -> `refarm runtime restart`. Defect 2 is what makes this silent; with a working staleness check this one is loud.",
		"DURABILITY GAP 1: `.superpowers/sdd/` is GITIGNORED. Fifty-one files and thirty-three subagent reports from 2026-08-04/05 exist only on this machine. Every measurement, every rejected alternative and every 'did not survive contact with the code' finding lives there and in commit messages -- nowhere else. A new session gets the commit messages, which are deliberately thorough for exactly this reason, and nothing more.",
		"DURABILITY GAP 2: there has never been a requirements interview. The operator asked on 2026-08-05 whether his needs are mapped and specified. They are NOT, as a document. What exists is inference spread across specs, docs/CONVERGENCE-LANE.md, and the assistant's own memory in ~/.claude/ -- which lives OUTSIDE the repository and does not travel with the project. Anything a future session knows about what he wants, it knows by reading conversation or by guessing.",
		"The main branch still has NO protection: gh api repos/:owner/:repo/branches/main/protection returns 404. No required PR, no required status checks. GitHub configuration only the operator can apply, and it wants applying before any promotion PR merges."
	],
	"next_actions": [
		"LOOSE ENDS QUEUE, opened at the operator's request on 2026-08-06 -- obvious fixes to be resolved definitively rather than decided one at a time. Each is named with its file so none needs rediscovering: (1) MODEL_BUDGET_X_USD=nan silently disables the budget guard with NO telemetry -- `nan` parses as a valid f64, NaN <= 0.0 is false so the zero short-circuit misses it, every >= against NaN is false so it never blocks, and because it lands in Known rather than Unknown no agent:budget:unknown event fires. Indistinguishable from a provider legitimately under budget. Fix: reject non-finite budgets at parse time (packages/agent/src/session/wasm_ops.rs). An unparseable budget string is the same shape -- it also returns false silently. (2) The budget re-query is unbounded and on a hot path: query-nodes filters by node TYPE not by provider, and node_reap.rs's REAPABLE_TYPES omits UsageRecord so it is never pruned, so system-wide volume drives it and once tripped the full fetch is a permanent per-call cost. Ways out: host-side summing, or a provider-scoped query. (3) Eight call sites still discard `truncated` -- every site this plan touched except the guard. (4) GET /tasks (sidecar/mod.rs:1718,1746,1752) still holds the unlimited query, a second sort order and a silent truncate, and DISAGREES with its guest sibling about 'the newest N tasks'. (5) No paging past MAX_NODES_PER_RESPONSE -- rows beyond it are unreachable at any limit. (6) The TS graph client throws against the real daemon because the sidecar never sets @context. (7) apps/farmhand/src/transports/sessions.ts:83-88 re-sorts by created_at_ns; harmless while the store is an in-memory Map, reintroduces the removed defect verbatim when Phase 2 swaps in storage-sqlite. Close it BEFORE that swap. (8) task_tools.rs:44 limits TaskEvent globally then filters by task_id, so an older task reports zero events while the sidecar reports all. (9) Diagram drift is ungated everywhere: local now computes and can fail, CI disables strictness explicitly. Root cause is SVG output not being reproducible across browser versions. Order: pin the browser locally as CI does, verify byte-stability, THEN decide about CI -- that last step is the operator's, it can break the pipeline.",
		"DIAGRAM DRIFT IS UNGATED, and the operator has a decision to make. Verified: the CI workflow sets REFARM_DIAGRAM_SYNC_STRICT=0, which check-diagrams.mjs:137-139 treats as advisory -- it warns and returns before process.exit(1). So drift fails nothing in CI, and before 2026-08-06 it was not even computed locally. `diagrams:check` now runs --ci with no baked override, so it computes and CAN fail; on this machine it exits 1 on ~35 SVGs because the local browser differs from the one CI pins via scripts/ci/install-puppeteer-browser.mjs. That failure is genuine drift genuinely observed. ORDER OF OPERATIONS: pin the same browser locally FIRST, verify byte-stability, and only then decide whether CI should stop being advisory. That last part is the operator's call, not an agent's -- it could break the pipeline.",
		"GET /tasks is the sharpest remaining instance of the shape this work removed, and it now DISAGREES with its own guest sibling. packages/tractor/src/sidecar/mod.rs:1718 does an unlimited query_nodes(\"Task\"), :1746 re-sorts by created_at_ns DESC, and :1752 silently truncates to limit.min(100) while answering {tasks, total} with no stored/truncated. The guest's list_tasks now takes the front N in updated_at order through the fixed bridge, so for any task whose status has changed the two surfaces name different 'newest N'. Same created-versus-touched divergence just fixed for Session, in the same file as the get_nodes that was fixed.",
		"Farmhand re-sorts Sessions by created_at_ns (apps/farmhand/src/transports/sessions.ts:83-88). Harmless today because that store is an in-memory Map where sessions are never upserted -- but apps/farmhand/src/index.ts:82 says it becomes @refarm.dev/storage-sqlite in Phase 2, at which point it reintroduces the removed defect verbatim. Close it BEFORE that swap, not after.",
		"Deferred from the record-reader plan, triaged, each its own ticket: (a) packages/sidecar-client reads only body.nodes and DISCARDS stored/truncated, with its own default limit equal to the ceiling -- every TS consumer of the graph client is back to a truncated answer it cannot distinguish from a whole one; (b) the WASM bridge (host/wasi_bridge/core.rs:378-386) still does query_nodes + .take(limit), the full materialisation the fix exists to remove, and its WIT returns a bare list<string> so the guest gets no truncation signal at all -- this is the same silent ceiling one layer lower, plugin-visible rather than operator-visible, and the fix agent named it as what it would prioritise next; (c) there is no paging past MAX_NODES_PER_RESPONSE, so rows beyond 100 are unreachable at any limit and the response does not say so; (d) wasm_ops.rs's max_by_key(created_at_ns) sits beside SQL's updated_at DESC -- session rows are upserted on append, so newest-touched is not newest-created and the top-N-by-touch window can exclude a newest-created session. Two sort orders for one fact, which the new document itself forbids.",
		"Plan 2 of the cockpit: the isolated launcher and parity (spec D3, D4). Designed, no plan written yet, deliberately sequenced second because plan 1 is what makes 'the sandbox works' provable rather than assertable. It carries its own graph so an experiment's cost lands in the sandbox's BudgetObservation record instead of the operator's real ledger -- not hypothetical, the workspace-attribution proofs on 2026-08-05 wrote test dispatches into the real record because there was nowhere else for them.",
		"Deferred from the cockpit plan, triaged, each its own ticket: (a) /proc/<pid>/environ is readable and would witness the daemon's OWN namespace and base -- today `refarm context` reports the CLI shell's values as if they were the node's, which is a reconstruction where a witness exists; (b) spec D2's third clause is UNDELIVERED -- nothing compares what the TypeScript stack resolves against what the Rust host reads from SOVEREIGN_BASE/SOVEREIGN_DIR, so that divergence surfaces only incidentally; (c) defaultHasMonorepoMarker duplicates a private predicate in @refarm.dev/config that needs a public export; (d) `node-not-running` is silent because runtime:not-ready covers the common case, but the two are structurally different signals and a stale descriptor beside a reachable sidecar is reported by neither -- disclosed in the module header, not hidden.",
		"KNOWN DIVERGENCE ON THIS NODE, and the new instrument is what reports it: the daemon loads plugin 22dbabbd while packages/agent/dist/agent.wasm builds to 544ef5b4. Commit 7a7668fc (wasm_ops.rs) landed after the daemon booted. Close it with `pnpm --filter @refarm.dev/agent run build` -> `refarm plugin install` -> `refarm runtime restart`. Deliberately not done for the operator: a restart interrupts whatever the node is serving.",
		"The workspace axis is populated but the QUERY still does not exist. `refarm budget summary --by workspace|node|surface|model` is now worth building because the record it reads finally carries the axis -- but FIX THE ORDERING BLOCKER FIRST, or the summary inherits a reader that cannot see recent records.",
		"Deferred from the workspace-attribution plan, triaged, NOT done. Own ticket each: (a) `--workspace` cannot correct an already-stamped session -- wasm_ops.rs:130-146 stamps only at creation, while the design asserts three times that the flag re-declares; a wrongly-seeded session stays wrong for life. (b) workspace_source never reaches the BudgetObservation -- dispatch.rs:516 already folds a budget ceiling by workspace_id with no provenance, so a cwd seed silently selects policy, which is exactly what the design calls breaching H2 in substance. (c) every session created BEFORE this feature has no stored declaration, so degree 3 re-fires from the current directory on every run. (d) the trim asymmetry between prompt_handler.rs:319-325 and dispatch.rs:205-215. (e) the /sessions double fetch with --session. (f) declaredWorkspaceRoots resolving via declaredBase(), which is why cross-workspace seeding is unreliable -- deserves a decision, not a patch. (g) the read-failure->unknown branch has no test; it needs injectability and it is safety-critical.",
		"KNOWN DIVERGENCE, source vs installed binary: the final fix wave changed declared_workspace() in packages/agent/src/session/wasm_ops.rs to require BOTH workspace_id and workspace_source or neither, and did NOT rebuild. ~/.refarm/plugins/refarm_agent/plugin.wasm and packages/agent/dist/agent.wasm both hash 22dbabbd..., which is the pre-fix-wave build. Unreachable from the CLI (the CLI always sends both), so nothing the operator does can hit it. Close it with `pnpm --filter @refarm.dev/agent run build` -> `refarm plugin install` -> `refarm runtime restart`.",
		"The fourth axis (quota). It governs the operator's actual daily route, needs no new counter, and the budget fold that would carry it is three-level with a hardcoded signature -- 3 Rust callers plus one TS mirror in budget-contract-v1/src/resolve.ts, guarded by conformance. Adding a level is contained but not free, and it is the same fold an automation-scoped budget would later need.",
		"A cost aggregation surface. The axes are already on every observation; what is missing is the question. Smallest honest version: `refarm budget summary --by workspace|node|surface|model` over the record that already exists, with the subscription case reported as quota-consuming rather than as zero dollars, since 0 is true and useless.",
		"The requirements interview the operator asked for. It should produce a document IN the repository, not in assistant memory, and it should be run against what is already built rather than from a blank page -- most of the material exists and is fragmented, which is a different problem from missing material.",
		"What executes a declared automation (spec 2026-08-05, three customers, two of them node-scoped). The declaration language is COMPLETE -- cron, one-shot, event, manual -- and nothing evaluates any of it; no cron parser exists anywhere in the tree. Blocked on nothing but a decision about where a node-scoped automation is written."
	],
	"open_questions": [
		"Should a divergence the agent is FORBIDDEN to fix ever block the agent gate? `refarm check` now receives the divergences, but they are warnings and only block under --fail-on-warnings. That is deliberate: the finding's own action says restarting is the operator's call, and a gate blocking on a condition the loop may not remedy would deadlock it and create an incentive to bypass the gate. Recorded as policy rather than settled -- it deserves its own decision.",
		"Should the budget record cover work refarm did not dispatch? The workspace-attribution plan itself cost ~1.5M subagent tokens across 17 dispatches, and NONE of it is in the BudgetObservation record -- that record covers `refarm ask` dispatches, not other tools. The operator's stated goal is to account for everything. Either refarm accepts observations from outside over a wire contract, or it admits it measures only what it dispatches. The answer shapes the fourth axis too.",
		"Does the bench need a correctness axis richer than substring? `--expect` grades work whose answer is checkable that way and nothing else. An automation with a known expected result is a scenario WITH a grader, which is the cheap path; a judge model is the expensive one. Neither is built.",
		"Where does a NODE-scoped automation live? .project/automations.json is project-scoped and the governed writer says so in its name. Two of the three known customers -- restarting this node, verifying this node's catalog -- are node concerns. The sovereign dir is the obvious home, beside config.json, node.json and model-rates.v1.json.",
		"Which cron parser? None exists, this repo is deliberately dependency-light, and cron is subtly easy to get wrong (day-of-week versus day-of-month). Vendoring something tested likely beats hand-rolling, and the choice deserves the same source-and-date discipline every other external dependency here now carries.",
		"rcdc5 as a workspace-scoped automation consumer is BLOCKED on two named pieces, and the blocker is a declared principle rather than an oversight: pending_prompt.rs states P1, `a pending prompt's lifetime is its asker's lifetime, nothing persists`, and nothing resumes a persisted non-terminal effort. Until both change, an automation that waits for an MFA push cannot survive a restart, and 'waiting for a human' is indistinguishable from 'dead'."
	],
	"key_decisions_active": [
		"DEC-031: host unificado refarm com renderers TUI/Web/headless sobre contratos compartilhados.",
		"DEC-032: produto CLI/host em apps/; packages/ guardam blocos reutilizáveis.",
		"DEC-034: artefatos WASM Rust devem ser copiados para dist/ durante build.",
		"DEC-035: provision para, configure ainda não existe — silo é o ponto de integração.",
		"DEC-036 [DECIDED]: refarm chat é cliente HTTP do sidecar farmhand em :42001. Não embeda farmhand.",
		"ADR-064 [ACCEPTED]: TokenAuthError com provider + reason + rotationUrl — produzido na camada de credenciais, renderizado por qualquer superfície.",
		"ADR-065 [ACCEPTED]: Farmhand transparent lifecycle — refarm é o single entry point. Auto-start com Y/n confirm, detached spawn, 300ms poll. LaunchDeps interface para testabilidade."
	],
	"files_in_flux": [
		"The guest-can-tell plan is COMPLETE and reviewed on develop, 1a17fbd4..4c59450f, 10 commits. Node in sync at plugin hash 50483c6c.",
		"UNTRACKED AND UNCOMMITTED: docs/OPERATOR_REQUIREMENTS.md, 430 lines, written 2026-08-06 09:41, claiming to record the first explicit requirements interview with the operator -- which is exactly what blocker 2 says has never existed. It was NOT authored by this session and its provenance is unverified from here, so it has deliberately not been committed. The operator should decide: it is his document or it records his words, and an uncommitted 430-line file answering a standing blocker is one `git clean` from gone.",
		"The two-installers fix was dispatched and unverified at checkpoint time -- confirm which directory it converged on and that the operator's node still boots.",
		"packages/prompt-contract-v1/src/index.ts - notice shape, say(), hub announce (watermark correction pending)",
		"packages/tractor/src/sidecar/pending_prompt.rs - the notice ring and POST /notices; also the home of P1, which the rcdc5 automation question now depends on"
	]
}
