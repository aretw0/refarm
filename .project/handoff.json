{
  "context": "Checkpoint 2026-07-24 (23rd). COLD-BOOTSTRAP landed + proven: a device with only Node installs the whole farm-client kit over the mesh with NO git/clone/npm — 'curl -fsSL http://serpro-1577853:4321/install.mjs | node --input-type=module -'. dist publish auto-detects the farm's MagicDNS name (tailscale Self.HostName) and bakes it into a self-contained install.mjs. Proven: curl-pipe pulled the 14-file kit into a bare dir and farm-ask drove the farm, host remembered. This session's farm-client arc (all zero-dep, all proven live over the mesh): usage footer → error-hint (plan-gated) → kit-remembers-the-farm → progress spinner → cold-bootstrap. Commits pushed 79e534fd. NEW: operator shared the north star — refarm = single control plane for their WHOLE life (personal+professional), a personal agentic OS; both their computers now on the tailnet.",
  "timestamp": "2026-07-24T20:36:30.719Z",
  "current_phase": "12",
  "current_tasks": [],
  "blockers": [],
  "next_actions": [
    "PWA on the phone (ladder step 3): audit apps/me for installable-PWA pieces (web manifest + service worker + offline), then serve via 'refarm web serve apps/me/dist --tls' — installable hub over the mesh. PWA, not APK (no native blocks; reuse apps/me + web serve).",
    "Cloudflare Tunnel (ladder step 4): real hostname + TLS (fixes PWA secure-context + reach from any network), reuses packages/infra-cloudflare. Railway = persistent always-on node, later. Operator to decide Cloudflare vs Railway — recommend Cloudflare first.",
    "x86 tractor node (ladder step 5): the personal PC (now on the tailnet) as a farm node via the same manifest + a platform field. Also the natural payload-2 for dist."
  ],
  "open_questions": [
    "Cloudflare vs Railway for raising nodes — recommend Cloudflare first (tunnel/TLS/edge, existing block); Railway for persistent compute later."
  ],
  "key_decisions_active": [
    "DEC-031: host unificado refarm com renderers TUI/Web/headless sobre contratos compartilhados.",
    "DEC-032: produto CLI/host em apps/; packages/ guardam blocos reutilizáveis.",
    "DEC-034: artefatos WASM Rust devem ser copiados para dist/ durante build.",
    "DEC-035: provision para, configure ainda não existe — silo é o ponto de integração.",
    "DEC-036 [DECIDED]: refarm chat é cliente HTTP do sidecar farmhand em :42001. Não embeda farmhand.",
    "ADR-064 [ACCEPTED]: TokenAuthError com provider + reason + rotationUrl — produzido na camada de credenciais, renderizado por qualquer superfície.",
    "ADR-065 [ACCEPTED]: Farmhand transparent lifecycle — refarm é o single entry point. Auto-start com Y/n confirm, detached spawn, 300ms poll. LaunchDeps interface para testabilidade."
  ],
  "files_in_flux": []
}
