# {{name}}

{{description}}

This validation scaffolds an Astro/WASI SSR proof surface with deterministic
componentization evidence.

```bash
pnpm -C validations/{{name}} run build
pnpm -C validations/{{name}} run componentize
pnpm -C validations/{{name}} run test
```
