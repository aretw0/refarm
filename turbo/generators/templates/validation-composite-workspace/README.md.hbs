# {{name}}

{{description}}

This validation is a composite workspace scaffold. Use it when a proof needs a
root orchestrator plus at least one nested workspace participant.

```bash
pnpm -C validations/{{name}} run test
```
