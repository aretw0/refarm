import assert from "node:assert/strict";
import test from "node:test";

import { describeHost } from "./main.mjs";

test("nested host exposes a validation role", () => {
	assert.equal(describeHost().role, "host");
});
