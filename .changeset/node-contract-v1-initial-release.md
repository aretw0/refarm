---
"@refarm.dev/node-contract-v1": minor
---

Initial public release of `@refarm.dev/node-contract-v1`: the `node:v1` base `GraphNode` shape (JSON-LD `@type`/`@id`), the normalised wire form and the conversions between them. Published as a support package because `vault-contract-v1` depends on it at runtime; a lane that ships that must ship this.
