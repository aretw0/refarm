---
"@refarm.dev/quality-contract-v1": minor
---

Add the quality:v1 contract for declared quality/lint profiles, findings, reports, conformance, and a reference regex checker.

Includes `validateQualityReport` / `isQualityReport`: a pure validator for report envelopes produced by any checker, in any language — the symmetric counterpart of `validateTaskArtifactManifest` in artifact-contract-v1.
