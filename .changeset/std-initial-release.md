---
"@refarm.dev/std": minor
---

Initial public release of `@refarm.dev/std`: zero-dependency pure primitives — `slugify`, hex-digest shape and constant-time comparison, and the bind/surface guards that decide where a listener may open. Published as a support package because consumer-facing packages (`vault-contract-v1`) depend on it at runtime; a lane that ships those must ship this.
