---
"@refarm.dev/sync-contract-v1": minor
---

Initial public release of the sync capability contract

- **@refarm.dev/sync-contract-v1**: Sync capability contract (sync:v1) with conformance test
  suite, full TypeScript typings, README with examples, ESM-only, Node 22+ compatible.

Pre-publish baseline (split from `initial-contracts-release.md` on 2026-07-03): this package is
in the `kernel-candidates` lane, not `vault-seed-ready`. It must be first-published through its
own explicit lane; fold further pre-publication contract changes into this changeset instead of
stacking new ones.
