---
"@refarm.dev/identity-contract-v1": minor
---

Reserve optional session-derived identity handles in `identity:v1` for OPAQUE,
WebAuthn, and other protocol-owned authentication sessions without requiring an
early `identity:v2` capability.
