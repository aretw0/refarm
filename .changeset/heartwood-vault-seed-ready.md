---
"@refarm.dev/heartwood": minor
---

Initial public candidate for the Heartwood WASM crypto helper that backs Silo key operations in the `vault-seed-ready` lane, with an explicit typed package export for the JCO entrypoint.
