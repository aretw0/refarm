---
"@refarm.dev/vault-contract-v1": minor
---

Enter the `consumer-ready` release selection with a declared `consumerPull`. The proof target is corpus health over a consumer's own `records:v1` projection — cross-record defects (dangling relations, orphans, duplicates) that no per-note gate can see — while the consumer's privacy heuristics, folder policy, and PII vocabulary stay downstream.
