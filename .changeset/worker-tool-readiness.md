---
"@refarm.dev/cli": patch
---

Expose `assessWorkerToolReadiness()` and structured runtime-dispatch blockers for worker tool descriptors, so consumers can offer plan-only "agents as tools" without parsing validation strings.
