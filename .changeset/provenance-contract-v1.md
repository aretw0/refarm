---
"@refarm.dev/provenance-contract-v1": minor
---

Add the provenance:v1 contract — where an input came from: source channel (required), file/path/origin link, collection time, content fingerprint, license and privacy — with pure stamp/read/verify helpers and a conformance suite. Enters the `consumer-ready` release selection with a declared `consumerPull`: the proof target is provenance stamped and verified on a consumer's own input records, the first of which is a materials database kept by a Python engine (arch-engine), while source vocabulary and evidence policy stay downstream.
