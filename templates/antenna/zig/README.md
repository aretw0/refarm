# Refarm Antenna - Zig Template

This is a template for building a low-level, memory-efficient Antenna (HTTP Gateway) plugin for Refarm using Zig.

## Building

Requires Zig v0.11 or later.
Run `zig build` to produce the WASM component.
