# Refarm Antenna - Odin Template

This is a high-level, data-focused template for an Antenna (HTTP Gateway) plugin using Odin.

## Building

Requires the Odin compiler with WASI integration.
Run `odin build . -target:wasm32-wasi`
