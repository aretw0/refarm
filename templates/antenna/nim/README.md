# Refarm Antenna - Nim Template

This is an expressive Nim template for an Antenna (HTTP Gateway) plugin in Refarm.

## Building

Requires Nim 2.0+. Compile to C, then use clang to target `wasm32-wasi`.
