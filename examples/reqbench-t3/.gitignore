dist-web/
.astro/

# The root .gitignore ignores .dgk/ (runtime state like requirements.manifest.json).
# But sources.json + the shipped playbook are SAMPLES the analyst edits — keep them versioned.
!.dgk/
.dgk/*
!.dgk/sources.json
!.dgk/requirements-sync.playbook.json
