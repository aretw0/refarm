#!/usr/bin/env node
/**
 * setup-llm-keys — configure API keys for pi-agent / tractor LLM providers
 *
 * Stores keys in .refarm/.env (gitignored).
 * Run any time to check status or update keys.
 *
 *   node scripts/setup-llm-keys.mjs          # interactive setup
 *   node scripts/setup-llm-keys.mjs --status # show current config, no changes
 */

import { existsSync, readFileSync, writeFileSync, mkdirSync, statSync } from 'node:fs';
import { createInterface } from 'node:readline';
import { spawnSync } from 'node:child_process';
import { join } from 'node:path';

const ROOT = new URL('..', import.meta.url).pathname.replace(/\/$/, '');
const ENV_FILE = join(ROOT, '.refarm', '.env');

const c = {
  reset: '\x1b[0m', bold: '\x1b[1m', dim: '\x1b[2m',
  green: '\x1b[32m', red: '\x1b[31m', yellow: '\x1b[33m',
  cyan: '\x1b[36m', blue: '\x1b[34m', magenta: '\x1b[35m',
};

const STATUS_ONLY = process.argv.includes('--status');

// ── provider registry ─────────────────────────────────────────────────────────

const PROVIDERS = [
  {
    id: 'ANTHROPIC_API_KEY',
    label: 'Anthropic  ',
    prompt: 'Paste your Anthropic API key (sk-ant-...): ',
    keysUrl: 'https://console.anthropic.com/settings/keys',
    validate: validateAnthropicKey,
  },
  {
    id: 'OPENAI_API_KEY',
    label: 'OpenAI     ',
    prompt: 'Paste your OpenAI API key (sk-...): ',
    keysUrl: 'https://platform.openai.com/api-keys',
    validate: validateOpenAIKey,
  },
  {
    id: 'GROQ_API_KEY',
    label: 'Groq       ',
    prompt: 'Paste your Groq API key (gsk_...): ',
    keysUrl: 'https://console.groq.com/keys',
    validate: (k) => validateBearerKey(k, 'https://api.groq.com/openai/v1/models'),
  },
  {
    id: 'MISTRAL_API_KEY',
    label: 'Mistral    ',
    prompt: 'Paste your Mistral API key: ',
    keysUrl: 'https://console.mistral.ai/api-keys',
    validate: (k) => validateBearerKey(k, 'https://api.mistral.ai/v1/models'),
  },
  {
    id: 'XAI_API_KEY',
    label: 'xAI / Grok ',
    prompt: 'Paste your xAI API key (xai-...): ',
    keysUrl: 'https://console.x.ai',
    validate: (k) => validateBearerKey(k, 'https://api.x.ai/v1/models'),
  },
  {
    id: 'DEEPSEEK_API_KEY',
    label: 'DeepSeek   ',
    prompt: 'Paste your DeepSeek API key: ',
    keysUrl: 'https://platform.deepseek.com/api_keys',
    validate: (k) => validateBearerKey(k, 'https://api.deepseek.com/models'),
  },
  {
    id: 'TOGETHER_API_KEY',
    label: 'Together   ',
    prompt: 'Paste your Together AI API key: ',
    keysUrl: 'https://api.together.xyz/settings/api-keys',
    validate: (k) => validateBearerKey(k, 'https://api.together.xyz/v1/models'),
  },
  {
    id: 'OPENROUTER_API_KEY',
    label: 'OpenRouter ',
    prompt: 'Paste your OpenRouter API key (sk-or-...): ',
    keysUrl: 'https://openrouter.ai/keys',
    validate: (k) => validateBearerKey(k, 'https://openrouter.ai/api/v1/models'),
  },
  {
    id: 'GEMINI_API_KEY',
    label: 'Gemini     ',
    prompt: 'Paste your Gemini API key (AIza...): ',
    keysUrl: 'https://aistudio.google.com/app/apikey',
    validate: (k) => validateBearerKey(k, 'https://generativelanguage.googleapis.com/v1beta/openai/models'),
  },
];

// ── helpers ───────────────────────────────────────────────────────────────────

function readEnvFile() {
  if (!existsSync(ENV_FILE)) return {};
  const lines = readFileSync(ENV_FILE, 'utf8').split('\n');
  const vars = {};
  for (const line of lines) {
    const trimmed = line.trim();
    if (!trimmed || trimmed.startsWith('#')) continue;
    const eq = trimmed.indexOf('=');
    if (eq < 0) continue;
    vars[trimmed.slice(0, eq)] = trimmed.slice(eq + 1);
  }
  return vars;
}

function writeEnvFile(vars) {
  mkdirSync(join(ROOT, '.refarm'), { recursive: true });
  const now = new Date().toISOString();
  const lines = [
    `# Refarm LLM provider keys — DO NOT COMMIT`,
    `# Generated by scripts/setup-llm-keys.mjs on ${now}`,
    ``,
  ];
  for (const [k, v] of Object.entries(vars)) {
    lines.push(`${k}=${v}`);
  }
  writeFileSync(ENV_FILE, lines.join('\n') + '\n');
}

function maskedKey(key) {
  if (!key || key.length < 12) return '(empty)';
  return key.slice(0, 10) + '...' + key.slice(-4);
}

function envFileMtime() {
  try { return statSync(ENV_FILE).mtime; } catch { return null; }
}

async function prompt(rl, question) {
  return new Promise(resolve => rl.question(question, resolve));
}

// ── validators ────────────────────────────────────────────────────────────────

async function validateAnthropicKey(key) {
  if (!key || !key.startsWith('sk-ant-')) return { ok: false, reason: 'invalid prefix (expected sk-ant-)' };
  const r = spawnSync('curl', [
    '-sf', '--max-time', '5',
    '-H', `x-api-key: ${key}`,
    '-H', 'anthropic-version: 2023-06-01',
    '-H', 'content-type: application/json',
    '-d', JSON.stringify({ model: 'claude-haiku-4-5-20251001', max_tokens: 1, messages: [{ role: 'user', content: 'hi' }] }),
    'https://api.anthropic.com/v1/messages',
  ], { encoding: 'utf8' });

  if (r.status !== 0) return { ok: false, reason: 'network error or curl unavailable' };
  try {
    const body = JSON.parse(r.stdout);
    if (body.error?.type === 'authentication_error') return { ok: false, reason: 'key rejected by Anthropic API' };
    if (body.id || body.content) return { ok: true };
    return { ok: false, reason: body.error?.message ?? 'unexpected response' };
  } catch {
    return { ok: false, reason: 'could not parse API response' };
  }
}

async function validateOpenAIKey(key) {
  if (!key || !key.startsWith('sk-')) return { ok: false, reason: 'invalid prefix (expected sk-)' };
  const r = spawnSync('curl', [
    '-sf', '--max-time', '5',
    '-H', `Authorization: Bearer ${key}`,
    'https://api.openai.com/v1/models',
  ], { encoding: 'utf8' });

  if (r.status !== 0) return { ok: false, reason: 'network error or curl unavailable' };
  try {
    const body = JSON.parse(r.stdout);
    if (body.error?.code === 'invalid_api_key') return { ok: false, reason: 'key rejected by OpenAI API' };
    if (Array.isArray(body.data)) return { ok: true };
    return { ok: false, reason: body.error?.message ?? 'unexpected response' };
  } catch {
    return { ok: false, reason: 'could not parse API response' };
  }
}

async function validateBearerKey(key, modelsUrl) {
  if (!key || key.length < 8) return { ok: false, reason: 'key too short' };
  const r = spawnSync('curl', [
    '-sf', '--max-time', '5',
    '-H', `Authorization: Bearer ${key}`,
    modelsUrl,
  ], { encoding: 'utf8' });

  if (r.status !== 0) return { ok: false, reason: 'network error or curl unavailable' };
  try {
    const body = JSON.parse(r.stdout);
    if (body.error) return { ok: false, reason: body.error.message ?? 'key rejected' };
    if (Array.isArray(body.data) || Array.isArray(body.models)) return { ok: true };
    return { ok: false, reason: 'unexpected response' };
  } catch {
    return { ok: false, reason: 'could not parse API response' };
  }
}

// ── display ───────────────────────────────────────────────────────────────────

function printHeader() {
  console.log(`\n${c.bold}${c.cyan}Refarm LLM Key Setup${c.reset}`);
  console.log(`${c.dim}Keys are stored in ${ENV_FILE.replace(ROOT, '.')} (gitignored)${c.reset}\n`);
}

function printProvider(label, key, valid, reason) {
  const icon = !key ? 'o' : valid ? '+' : 'x';
  const col = !key ? c.dim : valid ? c.green : c.red;
  const status = !key ? 'not configured' : valid ? 'valid' : `invalid — ${reason}`;
  console.log(`  ${col}[${icon}]${c.reset} ${c.bold}${label}${c.reset}  ${c.dim}${maskedKey(key)}${c.reset}  ${col}${status}${c.reset}`);
}

function openBrowser(url) {
  const opener = process.platform === 'darwin' ? 'open'
    : process.platform === 'win32' ? 'start'
    : 'xdg-open';
  spawnSync(opener, [url], { stdio: 'ignore' });
}

function printUsage(vars) {
  const provider = vars.LLM_PROVIDER ?? 'ollama';
  console.log(`\n${c.dim}To start pi-agent:${c.reset}`);
  console.log(`  ${c.cyan}source .refarm/.env${c.reset}`);
  console.log(`  ${c.cyan}export LLM_PROVIDER=${provider}${c.reset}`);
  console.log(`\n${c.dim}Or use the wrapper:${c.reset}  ${c.cyan}npm run agent:start${c.reset}\n`);
}

function printKeyAge() {
  const mtime = envFileMtime();
  if (!mtime) return;
  const days = Math.floor((Date.now() - mtime.getTime()) / (1000 * 60 * 60 * 24));
  const col = days > 180 ? c.yellow : c.dim;
  console.log(`  ${col}Keys last updated ${days} day(s) ago (${mtime.toISOString().slice(0, 10)})${c.reset}`);
  if (days > 180) {
    console.log(`  ${c.yellow}  Consider rotating keys for security hygiene (> 6 months old)${c.reset}`);
  }
}

// ── main ──────────────────────────────────────────────────────────────────────

async function main() {
  printHeader();

  const existing = readEnvFile();

  // Resolve keys: .refarm/.env takes precedence over process.env
  const keys = {};
  for (const p of PROVIDERS) {
    keys[p.id] = existing[p.id] ?? process.env[p.id] ?? '';
  }

  // Validate all configured keys in parallel
  process.stdout.write(`${c.dim}Checking configured keys...${c.reset}\n`);
  const results = await Promise.all(
    PROVIDERS.map(p => keys[p.id]
      ? p.validate(keys[p.id])
      : Promise.resolve({ ok: false, reason: 'not configured' })
    )
  );

  for (let i = 0; i < PROVIDERS.length; i++) {
    printProvider(PROVIDERS[i].label, keys[PROVIDERS[i].id], results[i].ok, results[i].reason);
  }
  printKeyAge();

  const configuredCount = results.filter(r => r.ok).length;
  console.log(`\n${c.dim}${configuredCount}/${PROVIDERS.length} providers configured${c.reset}`);

  if (STATUS_ONLY) {
    if (configuredCount === 0) {
      console.log(`${c.yellow}Run without --status to configure keys.${c.reset}\n`);
    }
    return;
  }

  // Interactive setup for providers that aren't valid
  const needsSetup = PROVIDERS.filter((_, i) => !results[i].ok);
  if (needsSetup.length === 0) {
    console.log(`\n${c.green}All configured providers are valid. Nothing to do.${c.reset}`);
    printUsage(existing);
    return;
  }

  console.log(`\n${c.bold}Configure keys${c.reset} ${c.dim}(press Enter to skip)${c.reset}\n`);

  const rl = createInterface({ input: process.stdin, output: process.stdout });
  const updated = { ...existing };

  for (const p of needsSetup) {
    console.log(`  ${c.dim}Opening browser → ${p.keysUrl}${c.reset}`);
    openBrowser(p.keysUrl);
    const input = (await prompt(rl, `  ${p.prompt}`)).trim();
    if (input) {
      process.stdout.write(`  ${c.dim}Validating...${c.reset} `);
      const r = await p.validate(input);
      if (r.ok) {
        console.log(`${c.green}+ valid${c.reset}`);
        updated[p.id] = input;
      } else {
        console.log(`${c.red}x ${r.reason}${c.reset}`);
        console.log(`  ${c.dim}Key NOT saved. Re-run to try again.${c.reset}`);
      }
    }
  }

  rl.close();

  const changed = JSON.stringify(updated) !== JSON.stringify(existing);
  if (changed) {
    writeEnvFile(updated);
    console.log(`\n${c.green}Saved to ${ENV_FILE.replace(ROOT + '/', '')}${c.reset}`);
  } else {
    console.log(`\n${c.dim}No changes made.${c.reset}`);
  }

  printUsage(updated);
}

main().catch(e => { console.error(e); process.exit(1); });
